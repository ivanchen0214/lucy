{
 "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "axios": "^0.20.0"
  }
}
